# MOVOS API Inventory v1.0

**Atlas version:** v1.0 · **Generated:** 2026-07-24 · **Repository HEAD:** `main` @ `bfea8db`
**Updated:** 2026-07-27 — CAP-002 (charging core CRUD) added, see note below.
**Updated:** 2026-07-30 — WO-ARGOS-007 (CAP-003) added 3 OCPP device-provisioning HTTP endpoints. The OCPP protocol traffic itself (BootNotification/Heartbeat/StatusNotification) is not HTTP — it runs over a WebSocket transport (`wss://<host>/ocpp/:ocppIdentity`) and is intentionally not listed in the table below, which only covers `/api/v1` HTTP routes; see [OCPP Engine Guide](../engineering/OCPP_ENGINE_GUIDE.md).
**Part of:** [MOVOS Product Atlas](./MOVOS_PRODUCT_ATLAS_v1.0.md)

All 28 real HTTP endpoints in `apps/movos-api`, prefix `/api/v1` (set in `src/main.ts`). This is the complete set — confirmed by reading every module import in `src/app.module.ts`; no partial or stub routes exist beyond these. (The OCPP WebSocket transport is a separate, non-HTTP surface — see the update note above.)

| Method | Route                                             | Purpose                           | Auth                                              | DTO                        | Controller                   | Service                   | Status           |
| ------ | ------------------------------------------------- | --------------------------------- | ------------------------------------------------- | -------------------------- | ---------------------------- | ------------------------- | ---------------- |
| POST   | `/auth/login`                                     | Authenticate                      | Local strategy + 5/min throttle                   | `LoginDto`                 | `AuthController`             | `AuthService`             | Live             |
| POST   | `/auth/refresh`                                   | Rotate refresh token              | httpOnly cookie                                   | —                          | `AuthController`             | `AuthService`             | Live             |
| POST   | `/auth/logout`                                    | Revoke session                    | JWT                                               | —                          | `AuthController`             | `AuthService`             | Live             |
| GET    | `/auth/me`                                        | Current user + orgs               | JWT                                               | —                          | `AuthController`             | `AuthService`             | Live             |
| POST   | `/auth/select-organization`                       | Scope token to org                | JWT                                               | `SelectOrgDto`             | `AuthController`             | `AuthService`             | Live             |
| GET    | `/organizations`                                  | List user's organizations         | JWT                                               | —                          | `OrganizationsController`    | `OrganizationsService`    | Live (read-only) |
| GET    | `/sites`                                          | List non-archived sites           | JWT + org context                                 | —                          | `SitesController`            | `SitesService`            | Live             |
| POST   | `/sites`                                          | Create site                       | JWT + OWNER/ADMIN                                 | `CreateSiteDto`            | `SitesController`            | `SitesService`            | Live             |
| GET    | `/sites/:id`                                      | Site detail                       | JWT + org context                                 | —                          | `SitesController`            | `SitesService`            | Live             |
| PATCH  | `/sites/:id`                                      | Update site                       | JWT + OWNER/ADMIN/OPERATOR                        | `UpdateSiteDto`            | `SitesController`            | `SitesService`            | Live             |
| POST   | `/sites/:id/archive`                              | Archive site                      | JWT + OWNER/ADMIN                                 | —                          | `SitesController`            | `SitesService`            | Live             |
| GET    | `/sites/:siteId/charging-stations`                | List charging stations for a site | JWT + org context                                 | —                          | `ChargingStationsController` | `ChargingStationsService` | Live (CAP-002)   |
| POST   | `/sites/:siteId/charging-stations`                | Create charging station           | JWT + OWNER/ADMIN                                 | `CreateChargingStationDto` | `ChargingStationsController` | `ChargingStationsService` | Live (CAP-002)   |
| GET    | `/charging-stations/:id`                          | Charging station detail           | JWT + org context                                 | —                          | `ChargingStationsController` | `ChargingStationsService` | Live (CAP-002)   |
| PATCH  | `/charging-stations/:id`                          | Update charging station           | JWT + OWNER/ADMIN/OPERATOR                        | `UpdateChargingStationDto` | `ChargingStationsController` | `ChargingStationsService` | Live (CAP-002)   |
| GET    | `/charging-stations/:chargingStationId/evses`     | List EVSEs for a charging station | JWT + org context                                 | —                          | `EvsesController`            | `EvsesService`            | Live (CAP-002)   |
| POST   | `/charging-stations/:chargingStationId/evses`     | Create EVSE                       | JWT + OWNER/ADMIN                                 | `CreateEvseDto`            | `EvsesController`            | `EvsesService`            | Live (CAP-002)   |
| GET    | `/evses/:id`                                      | EVSE detail                       | JWT + org context                                 | —                          | `EvsesController`            | `EvsesService`            | Live (CAP-002)   |
| PATCH  | `/evses/:id`                                      | Update EVSE                       | JWT + OWNER/ADMIN/OPERATOR                        | `UpdateEvseDto`            | `EvsesController`            | `EvsesService`            | Live (CAP-002)   |
| GET    | `/evses/:evseId/connectors`                       | List connectors for an EVSE       | JWT + org context                                 | —                          | `ConnectorsController`       | `ConnectorsService`       | Live (CAP-002)   |
| POST   | `/evses/:evseId/connectors`                       | Create connector                  | JWT + OWNER/ADMIN                                 | `CreateConnectorDto`       | `ConnectorsController`       | `ConnectorsService`       | Live (CAP-002)   |
| GET    | `/connectors/:id`                                 | Connector detail                  | JWT + org context                                 | —                          | `ConnectorsController`       | `ConnectorsService`       | Live (CAP-002)   |
| PATCH  | `/connectors/:id`                                 | Update connector                  | JWT + OWNER/ADMIN/OPERATOR                        | `UpdateConnectorDto`       | `ConnectorsController`       | `ConnectorsService`       | Live (CAP-002)   |
| GET    | `/locations/autocomplete`                         | Google Places autocomplete        | JWT, 30/min throttle                              | `AutocompleteQueryDto`     | `LocationController`         | `LocationService`         | Live             |
| GET    | `/locations/place/:placeId`                       | Resolve place → coordinates       | JWT, 30/min throttle                              | `PlaceQueryDto`            | `LocationController`         | `LocationService`         | Live             |
| POST   | `/charging-stations/:id/ocpp-provisioning`        | Provision OCPP identity + secret  | JWT + OWNER/ADMIN                                 | —                          | `OcppProvisioningController` | `OcppProvisioningService` | Live (CAP-003)   |
| POST   | `/charging-stations/:id/ocpp-provisioning/rotate` | Rotate OCPP secret                | JWT + OWNER/ADMIN                                 | —                          | `OcppProvisioningController` | `OcppProvisioningService` | Live (CAP-003)   |
| POST   | `/charging-stations/:id/ocpp-provisioning/revoke` | Revoke OCPP identity              | JWT + OWNER/ADMIN                                 | —                          | `OcppProvisioningController` | `OcppProvisioningService` | Live (CAP-003)   |
| GET    | `/health`                                         | Liveness probe                    | None (exempt from throttle via `@SkipThrottle()`) | —                          | `HealthController`           | —                         | Live             |

## CAP-002 scope note

The 12 charging-core endpoints above implement CRUD only — no create/read/update on any resource triggers OCPP communication, a charging session, a tariff calculation, a reservation, or a payment. `ChargingStation`/`Evse`/`Connector` support no hard delete; deactivation is status-based (`ChargingStationStatus.ARCHIVED`, etc.), matching the existing `Site` convention. See [CAP-002 Charging Terminology Mapping](../domain/CAP-002_CHARGING_TERMINOLOGY_MAPPING.md) for how these entities relate to the pre-existing `apps/movos-web` frontend types.

## CAP-003 scope note

The 3 `ocpp-provisioning` endpoints above only manage OCPP device credentials (`ocppIdentity` + secret) — they do not initiate a device connection, send an OCPP command, or return any live device state. Actual protocol traffic (BootNotification/Heartbeat/StatusNotification) flows over the separate WebSocket transport, not through `/api/v1`, and is not represented as HTTP endpoints in this inventory. No endpoint exists to read live connection status, force-disconnect a device, or send a remote command — none of that is in scope for this first vertical. See the [OCPP Engine Guide](../engineering/OCPP_ENGINE_GUIDE.md) and [Device Provisioning Guide](../engineering/OCPP_DEVICE_PROVISIONING_GUIDE.md).

## Frontend consumption (WO-ARGOS-004, 2026-07-28)

All 12 endpoints are now called from `apps/movos-web` via `src/lib/charging-api.ts`, a thin typed wrapper over the same `apiClient` used by Sites (auth header, `X-Organization-Id`, 401-refresh-retry all inherited automatically). No new endpoint was added or changed to support this — the frontend consumes exactly the 12 endpoints as specified above. One consequence worth noting: there is still no endpoint to list `ChargingStation`s (or EVSEs, or Connectors) across an entire organization — only per-parent (`GET /sites/:siteId/charging-stations`, etc.) — so the frontend's connected UI is necessarily Site-scoped (`/sites/[id]/charging-stations/...`), not a flat global list. The pre-existing flat mock pages (`/stations`, `/chargers`, `/connectors`) were left untouched for this reason; see [Screen Inventory](./MOVOS_SCREEN_INVENTORY_v1.0.md).

## Confirmed absent

No endpoints exist anywhere for: sessions, tariffs, alerts, reports, or team/user management. Charging stations, EVSEs, and connectors are now live (CAP-002) as CRUD-only resources, and device identity/authentication is now live (CAP-003) — but sessions, tariffs, reservations, RFID/authorization, and payments remain absent for all three. `AuditService` exists (`src/audit/`) but has no controller — it's a cross-cutting service invoked internally from each resource service (`SitesService`, `ChargingStationsService`, `EvsesService`, `ConnectorsService`, `AuthService`, `OcppProvisioningService`), not an API surface of its own.

## Cross-cutting middleware (applies to all routes above)

`ValidationPipe` (`whitelist: true, forbidNonWhitelisted: true`) · `HttpExceptionFilter` · `CorrelationIdInterceptor` · `RequestLoggerMiddleware` · `ThrottlerModule` global default (120 req/min, actually enforced as of PR #5 — see [CAP-001's updated note](../audits/CAP001_PRODUCT_READINESS_ASSESSMENT.md) on this fix).
