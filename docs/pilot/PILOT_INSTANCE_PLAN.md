# Pilot Instance Plan

**Work order:** WO-ARGOS-041
**Baseline:** production, verified live against `MOVOS_PILOT_ENVIRONMENT_READY` (`a22fb3c`), immediately after WO-ARGOS-040's controlled test data was fully removed (verified: 1 org, 1 user, 6 sites, 0 stations, 0 `WorkOrder`s).
**Shape:** per `docs/pilot/OPERATIONAL_PILOT_V1.md` (WO-ARGOS-039) — 1 organization, 1 site, 2–3 stations, 1 operator, 1 technician.

**Classification key:** `READY` (exists or is fully mechanical, needs no new information) · `NEEDS_REAL_INPUT` (the process/mechanism is ready, but a real identity, name, or decision from ARGOS/the business is required before it can be executed) · `MANUAL_PROVISIONING_REQUIRED` (mechanism proven in `PILOT_PROVISIONING_RUNBOOK.md`, executed once real inputs are known).

| Object                   | Status                                                  | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------ | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Organization**         | READY, pending confirmation                             | "Kylum Energy" (`kylum-energy`) already exists in production, created 2026-07-15. It has been this engagement's working name throughout (`tenant.ts`, seed defaults, the OCPP hardware questionnaire). Whether this is the actual pilot's organization identity, or a placeholder that should be renamed/replaced before real pilot data enters it, is a decision for ARGOS — not assumed here                                               |
| **Site**                 | NEEDS_REAL_INPUT                                        | 6 real `Site` rows already exist in production, but their names (`Test Location 007A`, `Estacion Google Maps Test`, `T4 GooglePlaces`, `T5 ManualAdj`, plus two "Centro Comercial Calim(a)" rows) read as QA/exploration artifacts from earlier `Sites` module development, not a deliberately chosen real pilot location. ARGOS must confirm: reuse one of these six, or provision a genuinely new site representing the real pilot address |
| **Stations (2–3)**       | MANUAL_PROVISIONING_REQUIRED once the site is confirmed | Zero `ChargingStation` rows exist in production today. The creation mechanism is real, tested, and already proven live during WO-ARGOS-040's controlled test — but real station names/identifiers are needed if this pilot is meant to represent actual physical or named equipment, rather than placeholder labels                                                                                                                          |
| **Operator User**        | NEEDS_REAL_INPUT                                        | One real user already exists (`alipise@gmail.com`, `OWNER`). Whether this same person plays the pilot's "operator" role, or a distinct real person should be provisioned instead, needs explicit confirmation — not assumed from existing data                                                                                                                                                                                               |
| **Technician User**      | NEEDS_REAL_INPUT                                        | No `TECHNICIAN` membership exists in production. This is the single most concrete missing input: a real name, email, and (for credential delivery) a real communication channel for an actual field technician                                                                                                                                                                                                                               |
| **Memberships/Roles**    | READY (mechanism)                                       | `Membership` creation with the correct `MemberRole` is a proven, working, one-time script step (`PILOT_PROVISIONING_RUNBOOK.md`) — blocked only on the real identities above, not on anything technical                                                                                                                                                                                                                                      |
| **Credentials**          | MANUAL_PROVISIONING_REQUIRED once identities are known  | Password generation and delivery is a solved, safe mechanism (demonstrated in WO-ARGOS-040 — a real password was generated, used, and never printed or persisted in plaintext anywhere). Delivery itself requires a real, secure channel to the real operator and technician, which requires their real identities first                                                                                                                     |
| **Required URLs/access** | READY                                                   | `https://movos-web.vercel.app` (frontend) and `https://movos-api-production.up.railway.app` (API, not directly visited by end users) are live, verified, and require no further preparation                                                                                                                                                                                                                                                  |

## What this plan deliberately does not do

No identity, site name, or station identifier is invented anywhere in this document or the runbook it feeds. Every `NEEDS_REAL_INPUT` row above is a literal question for ARGOS or the pilot business contact, not a placeholder to fill in later.
