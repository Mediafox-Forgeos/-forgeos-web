# @mediafox/typescript-config

Shared strict TypeScript baselines for MediaFOX Forge packages.

`base.json` is framework-neutral. `nextjs.json` adds only Next.js compiler requirements. Application aliases and include rules remain owned by each application.
