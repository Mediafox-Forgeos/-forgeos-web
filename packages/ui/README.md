# @mediafox/ui

Future home for low-level reusable UI primitives.

The existing Button, Card, Input, and styling utility remain inside `apps/forgeos-web` in Mission 002. Moving them now would require a Tailwind content and package-style strategy that risks changing the deployed UI. ForgeOS-specific components must remain in the web application.
