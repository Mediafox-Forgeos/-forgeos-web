# @mediafox/eslint-config

Shared ESLint support values for workspace applications.

The ForgeOS web application keeps its Next.js-specific `FlatCompat` setup locally, because `next/core-web-vitals` is application-framework configuration. This package currently centralizes only framework-neutral ignored build directories.
