# @mediafox/shared-types

Reserved for stable contracts shared by multiple applications or services.

ForgeOS view models remain in `apps/forgeos-web/types` because they are currently coupled to the web presentation. This package will receive types only after a second consumer exists.
