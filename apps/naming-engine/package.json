{
  "name": "@mediafox/naming-engine-cli",
  "version": "1.0.0",
  "description": "MediaFOX Forge naming engine CLI — thin wrapper over @mediafox/naming-engine",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "dev": "tsx src/cli.ts",
    "run:example": "tsx src/cli.ts --input example-input.json --output reports/output.md",
    "build": "tsc --outDir dist",
    "start": "node dist/cli.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@mediafox/naming-engine": "workspace:*"
  },
  "devDependencies": {
    "@mediafox/eslint-config": "workspace:*",
    "@mediafox/typescript-config": "workspace:*",
    "@types/node": "^22.10.1",
    "eslint": "^9.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
