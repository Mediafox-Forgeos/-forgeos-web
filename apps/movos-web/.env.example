NEXT_PUBLIC_MOVOS_API_URL=http://localhost:4000

# Google Maps (browser-side — restrict this key to your domain in the Cloud Console)
# Leave empty to hide the interactive map and fall back to manual coordinates.
NEXT_PUBLIC_GOOGLE_MAPS_BROWSER_API_KEY=
